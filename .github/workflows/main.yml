name: Self-hosted Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest frontend code and restart containers
        run: |
          cd ~/AdminPanelFront
          git pull origin main
          
          docker rm -f adminpanel-front || true
          
          docker compose down
          
          docker compose up -d --build
